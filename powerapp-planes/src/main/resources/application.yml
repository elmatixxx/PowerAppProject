server:
  port: 8083

spring:
  application:
    name: powerapp-planes
  datasource:
    url: jdbc:postgresql://localhost:5432/powerapp_planes
    username: powerapp
    password: powerapp
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info
