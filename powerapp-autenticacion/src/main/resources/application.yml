server:
  port: 8081

spring:
  application:
    name: powerapp-autenticacion
  datasource:
    url: jdbc:postgresql://localhost:5432/powerapp_autenticacion
    username: powerapp
    password: powerapp
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info
